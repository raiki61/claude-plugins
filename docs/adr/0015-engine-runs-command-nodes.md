# 0015. 走らせて写すだけの節は engine が走らせる（手順 3b）

- 状態: 採用
- 日付: 2026-09-25
- 決めた人: 作業メモに記載なし（計画の手順 3b として判定から入る run で入れた）
- 実装: 0.21.0 に入った（変異の腕を engine が撃つ段は未）

## 文脈

- テスト一式・CI・並行 PR の確認・規模の数値のように、コマンドを走らせて結果を写すだけの節を、安いモデルの任せ先（haiku）に任せていた。任せ先が数値やテスト結果を写し違えた（この日の 15 run のうち 13 run）。

## 決定

- 節の中の「走らせる・数える」を engine に、「読む・決める」を役に分ける。
- `p0.local_checks`・`p4.ci` は宣言のコマンドを engine が別プロセスで走らせ、終了コードから返答を組む。`p0.parallel_pr` の機械の段と `p4.scalars` も engine の節にする。これらの節は `done` を拒み、回す側や任せ先が結果を書く道を閉じる。

## 比べた案

作業メモに記載なし。

## 理由

任せ先が写し違える事故の根治（計画版の 1 章の事故 A09）。

## 結果

- haiku の任せ先は 0 本になった。宣言の無いリポジトリでは sonnet の任せ先に落ち、その結果は自己申告として収束の前に人に諮る。
- 残り: 変異テストの腕を engine が撃つ段（`p1.gate_efficacy`・`p3.delta_gates`・`p4.final_gates`）。

## 見直す条件

作業メモに記載なし。

## 出どころ

- リポジトリの中: 0.21.0（82deb2e）。[review-graph の手順書](../../graphloops/commands/review-graph.md)の engine_run の説明。
- 記録した時点の出どころ（リポジトリの外の作業メモ。消えうる）: 計画版の 3 章「手順 3b」、`HANDOFF-log-20260925.md` の手順 3b の run の行。
