# 0049. 仕様の流れを TDD の版の上の差し替え（extends の 2 段）にする

- 状態: 置き換え（0050 に）
- 日付: 2026-09-26
- 決めた人: 人（09:00、推し (b) のとおり）
- 実装: 当たらない（置き換え済み）

## 文脈

- 選べる流れの選び方が 2 通りに割れていた: TDD は `--loop review-loop-tdd`（extends で別の graph）、仕様は `--input flow=spec`（同じ graph の中の節）。gates=merge で入力が 3 本目になった。
- 案: (a) 仕様の流れを通常の流れの差し替え版にし `--loop` 1 通りに揃える（TDD と仕様の組み合わせは持てなくなる）／(b) 仕様の流れを TDD の版のさらに差し替え版にする／(c) TDD も入力に揃える（入力で graph の形を変える仕組みを engine に新設）／(d) 仕様の流れを合流から外す。

## 決定

- （置き換え済み）(b) 仕様の流れを TDD の版の上の差し替え（review-loop-spec.json extends review-loop-tdd.json）にする。flow=spec の入力と共有 graph への埋め込みは消す。手順 3 の前に 1 本の run で入れる。

## 比べた案

- 上の (a)・(c)・(d)。

## 理由

今できる「仕様＋TDD」の組み合わせを残せて（上位互換）、設計文書の案（仕様 → 判定 → TDD の修正 → 検算）とも合う。

## 結果

- 字面どおりだと、TDD を使わない仕様の run が消え、上位互換に反する。同じ日の 12:30 に回す側が部品の形へ改めた（[0050](0050-spec-flow-as-component.md)）。

## 見直す条件

作業メモに記載なし。

## 出どころ

- 記録した時点の出どころ（リポジトリの外の作業メモ。消えうる）: `MORNING.md` の 23:40 の「決めてほしいこと」5 と 09:00、`REPLAN-0926.md` の「人の答え（09:00）で順番に入る物」。
