# 0010. 直す依頼は判定から入る run に通す（直接直すのは誤字・コメント・文言だけ）

- 状態: 採用
- 日付: 2026-09-25
- 決めた人: 人（18:20）
- 実装: 0.20.0 に入口、0.20.2 に使い分け

## 文脈

- 0.20.0 で、人の修正依頼を P1 を飛ばして判定（p2.diagnose）へ流す入口を入れた。
- 0.20.1 を使う別の場所の AI が、この入口に気づきにくかった（18:40 の人の報告）。それまでの使い分けは「局所のバグは直接直せ」だった。

## 決定

- 直す依頼は、できるだけ判定から入る run に通す。ループを回さず直接直すのは、誤字・コメント・文言の直しだけ。

## 比べた案

- 局所のバグは直接直す（それまでの線引き）。

## 理由

- 作業メモに理由の記載なし。
- 手順書は、機構の新設・共有面の拡大に及ぶ直しは修正でなく設計作業で、零処方からの列挙なしに実装することを REVIEW.md の処方の最小性の観点が禁じている、と添えている（[手順書の「判定から入る」](../../graphloops/commands/review-graph.md#判定から入る人の修正依頼)）。

## 結果

- 判定役の反証・修正案の事前審査・修正差分の審査が依頼にも当たる。
- 代償: 判定から入る周は P1 の役を起こさないので、依頼の外を見る目（衛生・手順トレース・外部標準など）がその周に無い。

## 見直す条件

作業メモに記載なし。

## 出どころ

- リポジトリの中: 入口は 0.20.0（5dfb0bf）、使い分けと入口の気づきやすさは 0.20.2（32dfd94）。[手順書の「判定から入る」](../../graphloops/commands/review-graph.md#判定から入る人の修正依頼)。
- 記録した時点の出どころ（リポジトリの外の作業メモ。消えうる）: `HANDOFF-log-20260925.md` の 18:20・18:40、計画版の 2 章「起動と安全」。
