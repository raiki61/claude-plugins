# 0011. 重い工程のコマンドに disable-model-invocation を付け、提案だけする案内を別に置く

- 状態: 置き換え（0012 に）
- 日付: 2026-09-25
- 決めた人: 人（18:45。19:50 に取り下げ）
- 実装: 当たらない（置き換え済み）

## 文脈

- 重い工程（review-graph・research-graph・/review-loop・/research-loop）はトークンをかなり使うので、人が決めたときだけ回したい。コマンドはモデルからも呼べうる。

## 決定

- （取り下げ済み）本体のコマンドに `disable-model-invocation` を付け、起動する力を持たず提案だけする小さな案内を別に置く（案 iv）。

## 比べた案

作業メモに記載なし。

## 理由

トークンをかなり使うので、人が決めたときだけ回したい。

## 結果

- 同じ日の 19:50 に取り下げた。description が AI の文脈から消えて入口に気づけなくなり、人が言葉で「工程に回して」と頼んだときにも起動できなくなるため。後継は [0012](0012-heavy-processes-on-human-request.md)。

## 見直す条件

作業メモに記載なし。

## 出どころ

- 記録した時点の出どころ（リポジトリの外の作業メモ。消えうる）: `HANDOFF-log-20260925.md` の 18:45・19:50、計画版の 7 章の 10。
