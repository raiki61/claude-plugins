# 0043. Windows の見送りの許しの一覧に symlink を入れない

- 状態: 採用
- 日付: 2026-09-26
- 決めた人: 回す側の推し（14:50。人の「自明なものは推しで」の委任による）。15:10 に実機の CI で確かめて確定
- 実装: 作業枝で済み（0.21.0 には未収録）

## 文脈

- OS ごとの見送りの許し（[0042](0042-ci-skip-allowlist-per-os.md)）を作るとき、GitHub の Windows のランナーで symlink を作れるかが未確認だった。

## 決定

Windows の許しの一覧に symlink を入れない。

## 比べた案

- 許しの一覧に symlink を入れる。

## 理由

実機の CI（作業枝の push で回した run）で、symlink は見送りに出なかった。

## 結果

- 同じ日の CI の見送りの run の修正後の関所（r4.human_gate）が、この件を後退として聞き直し、R3・R4 も同じ件で作り直しを求めた（人の決定で答え済みの重複）。この置き場を作ったきっかけの 1 つ。

## 見直す条件

作業メモに記載なし。

## 出どころ

- 記録した時点の出どころ（リポジトリの外の作業メモ。消えうる）: `merge-request.md` の「ci-skip の残り」（14:50・15:10・19:00）、この記録の run の依頼。
