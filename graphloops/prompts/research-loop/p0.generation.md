# P0-7 断面の生成（回す側の仕事・重厚）

問い: {{record.question}}
制約: {{record.constraints | pick text}}
いまの主張: {{record.claims | pick id,cluster,claim,load_bearing}}
クラスタ: {{record.clusters}}

切り口が凡庸・手詰まりのとき、次の生成ヒューリスティクスを順に当て、出てきた断面・対抗仮説を本文に書かず主張の形にして返せ（生成は安いので数を撃ち、P1 の反証で濾す）:

- 不在の逆算（枯れているはずなのに、なぜ完成品が無いのか）
- 違和感の棚卸し（期待と観測のずれ）
- 抽象度の上下（どの抽象度で自分たち固有でなくなるか）
- 直交化（A が B に引きずられているなら空間・時間・条件で分離）
- 隣の構造の転用（内容でなく構造を借りる）
- 逆転（これが偽になる世界を記述）
- 極端化（パラメータを極端に振って適用条件の境界を出す）
- 用語の二義展開（多義語の読みごとに枝を分けて両方検証）

手筋は木に展開できる（MECE に割る→浅く広く→P1 の反証で枝を刈る→有望枝に予算）。heuristic にどの手筋から出たかを書く。cluster は既存の key か新しい key。
