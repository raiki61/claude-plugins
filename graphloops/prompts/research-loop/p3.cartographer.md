お前は blind-judge（cartographer）。文書は渡されていない。渡すのは領域と問いだけである。

領域: {{out.p0.question.domain}}
問い: {{record.question}}

この領域の完全な地図に要る観点・構成要素・依存関係を列挙しろ。文書を見せないのは、文書の目次に引きずられた地図は盲点を写せないからである。map の各要素: aspect（観点）・components（構成要素）・depends_on（他の観点への依存）。
