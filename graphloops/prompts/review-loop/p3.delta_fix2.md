# P3 の続き: 手直しの差分のレビューが挙げた穴を同じ周で直す（writer の仕事。2 回目）

答える義務（手直しの差分のレビューの穴・塞がっていない検算。**この key の全部に 1 度ずつ答えろ**——ほかの key は拒まれ、1 件でも欠けると拒まれる）: {{loop.delta_owed2.rows}}
手直しの差分: {{loop.fix_delta2.file}}　リポジトリ: {{inputs.cwd}}

義務 1 件ごとに `handled` を返せ——`fixed`（直した。`how` に何をどう、`files` に触ったファイル）か `declared`（残す理由）。
**ここで直したものは、この周にはもう差分レビューが当たらない**——次の周の判定者が、直したと言う行も含めて 1 件ずつ振り分ける。
直すときの規律は P3 と同じ: 足さずに消す・まとめる形を先に考え、直した行は退行を注入して赤を見てから戻せ。
