# P4-5 scalar の記録（writer の仕事）

リポジトリ: {{inputs.cwd}}　script の置き場: {{inputs.scripts_dir}}　BASE: {{record.base}}　変更ファイルの一覧: {{loop.changed_files_file}}（1 行 1 ファイル。要る所だけ Read）

規模指標を scalars に書け（名前 → 数値）。文書・手順書を含むなら当該文書の行数（doc_lines）、コードを含むなら `bash {{inputs.scripts_dir}}/comment-ratio.sh {{record.base}}` の出力（comment_ratio_pct 等。**手で数え直すな**——物差しが揺れると周の間の比較が成立しない）。scalar はゲートにしない——増加は R1 への入力であって閾値で止めるものではない。値は数で書け（文字列は検証器が拒む）。
