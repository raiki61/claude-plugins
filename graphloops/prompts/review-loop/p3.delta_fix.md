# P3 の続き: 修正差分のレビューが挙げた穴を同じ周で直す（writer の仕事）

答える義務（修正差分のレビューの穴・塞がっていない検算・証拠にならない修正差分の腕。**この key の全部に 1 度ずつ答えろ**——ほかの key は拒まれ、1 件でも欠けると拒まれる）: {{loop.delta_owed.rows}}
修正の差分: {{loop.fix_delta.file}}　リポジトリ: {{inputs.cwd}}

**この工程が在る理由**: 修正が作った穴を次の周まで持ち越すと、次の周の全体レビューがそれを新しい指摘として挙げ、
次の周の修正がまた次の穴を作る。同じ周のうちに閉じる。

義務 1 件ごとに `handled` を返せ（腕の行は、腕を足す・直して赤を見るか、残す理由を書く）:
- `fixed`: 直した。`how` に何をどう直したか、`files` に触ったファイル
- `declared`: 直さずに残す。`how` に残す理由（誤検知なら現物のどこで誤りと言えるか）

直すときも P3 と同じ規律: 足さずに消す・まとめる形を先に考え、直した行は退行を注入して赤を見てから戻せ。
