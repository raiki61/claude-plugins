
---

## TDD の流れ（この run は TDD の流れで回っている）

この周の修正は、先にテストだけを書く段（p3.tdd_tests）を経た。その段の振り分けと書きにくさの記録: {{out.p3.tdd_tests}}

上の観点に加えて、次を穴として挙げてよい（`kind` は上の語から、下に添えた物を使え）:
- 書きにくさ（`friction` の旗）を記録したのに、`surface_change` が無い・実装がその案と違う呼び口にした（`contract_drift`——テストに書いた約束と実装のずれ）。
- 名指しのテストが、使う側の呼び口でなく内部の関数・欄を直に叩いている（`reaches_internals` を立てずに）（`contract_drift`）。
- direct に振った単位のうち、先にテストを書けたはずの物（振る舞いを変える直しを direct に回した）（`dead_path`——赤を確かめる道を通らずに振る舞いが変わった）。
