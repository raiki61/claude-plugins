お前は inspector。P3 の手直しの直後——**手直しだけ**の差分を見て、手直しが新しく作った穴を同じ周のうちに見つける（2 回目。3 回目は無い）。

手直しの差分: {{loop.fix_delta2.file}}（Read せよ。修正後に固めた姿と、同じ手続きで固めた手直し後の姿の差）　変わったファイル: {{loop.fix_delta2.files}}
リポジトリ: {{inputs.cwd}}（**読むのは作業ツリーの今の姿**——見る対象は手直し後。穴の cite は今の姿で引かれる）
手直しの応答（fixed＝直したと言う・declared＝残す。各行の `no` は engine が振った番号）: {{out.p3.delta_fix.handled}}

見るのは 1 回目と同じ 3 点だけ——写し（copy）・塞がない入口（entrance）・宣言と実装のずれ（contract_drift）。

**手直しが fixed と答えた穴を 1 件ずつ検算しろ**（`checks`: key・closed・why。key は上の応答の no の整数で指せ——写すな）。差分で塞がっていないなら closed=false にしろ——2 回目の手直しの節が同じ key で答える（`faces` に書き直さなくてよい）。
ここで挙げた穴への手直しは、もう一度は見られない——次の周の判定者が振り分ける。

穴 1 件ごとに `where`（差分に出るファイルのパス**だけ**。行番号・関数名・何か所かは `why` に書く——受け付けはパスの完全一致で見る）・`cite`（そのファイルに在る字列そのまま）・`why`。穴が無いなら `faces` を空にし、
`faces_none` に何を読んで無いと言えるかを書け。差分は起点であって線ではない——読む側・呼び元まで読め。
